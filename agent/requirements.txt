# Grindly agent worker deps. Uses local Ollama via REST (no python client needed).
pdfminer.six>=20231228   # resume PDF text extraction
python-docx>=1.1.0       # resume DOCX text extraction
playwright>=1.44.0       # live platform scraping + apply (mode=live only)
cryptography>=42.0.0     # AES-256-GCM credential encryption (agent/secret_box.py)
psycopg2-binary>=2.9.9   # Postgres driver for the production queue (agent/db.py); unused on SQLite
scikit-learn>=1.4.0      # TF-IDF semantic scoring in matcher.py (cosine_similarity)
sentry-sdk>=2.0.0        # error tracking — only active when SENTRY_DSN env var is set
# Optional (email outcome scanner): google-auth google-auth-httplib2 google-api-python-client
